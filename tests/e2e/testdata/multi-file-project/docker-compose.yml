services:
  app:
    image: alpine:latest
    command: ["sleep", "3600"]
    environment:
      - APP_ENV=development
    depends_on:
      - db

  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: testdb
      POSTGRES_USER: test
      POSTGRES_PASSWORD: test
